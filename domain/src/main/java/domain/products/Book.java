package domain.products;

import domain.Product;

public class Book extends Product {

    public Book(String nameProduct, double price, double rate, int id) {
        super(nameProduct, price, rate, id);
    }
}
