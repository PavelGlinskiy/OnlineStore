package domain.products;

import domain.Product;

public class Dog extends Product {

    public Dog(String nameProduct, double price, double rate, int id) {
        super(nameProduct, price, rate, id);
    }
}
